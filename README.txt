BLASTTIER — COMPLETE FINAL BUILD

Upload every file in this folder and the assets/ folder to the GitHub Pages repository root.

Kits included:
LTMs, Vanilla, UHC, Pot, NethOP, SMP, Diamond SMP, Sword, Axe, Mace, Cart, SpearMace.

No player is pre-added. Storage uses a fresh key so old test/demo players do not appear.

Player list:
Skin -> Player -> Region -> Kit logo + tier -> next kit + tier.
Click a player to open the profile.

Profile:
Edit Player is the final button at the bottom.
Delete Player is NOT on the profile; it is inside Edit Player at the bottom.

Edit Player:
Player details + skin upload + repeated Kit/Tier rows + Add Kit + Tier + Save + Delete Player.

Cart and Diamond SMP use the logos supplied by the user.
SpearMace uses the custom mace+spear icon.
